# API Key：严禁入库（.env 含真实密钥，泄漏=账号被盗刷）
.env

# 运行产物（每批生成的数据集、对比报告、错误日志）—— 中间产物，不入库
#
# ⚠ 这里必须写 `data/*` 而不是 `data/`：被排除的**目录**无法再用 `!` 反选回来
# （gitignore 的规则是"父目录被排除时，其下文件不能重新包含"），
# 所以只能排除目录内容，再把要交付的那个子目录单独反选。
data/*

# 交付物例外：需求「成果形式」要求交「示例合成数据集（带数据卡）」，
# 故 data/export/ 下的精选集与数据卡必须入库。
# 另：仓库根 .gitignore 有全局 `*.jsonl`，但同目录下的 .gitignore 优先级更高，
# 这里的反选可以覆盖它 —— 因此无需 `git add -f`，也不必动队友的规则。
!data/export/
!data/export/*.jsonl
!data/export/*.md
!data/export/*.log

# 演示例外：只放行**这一份**对比存档（2026-09-11 那次真跑）。
#
# 为什么要入库：`--from-report` 是秒级重放已存档的对比结果，存档不在仓库里，
# 别人 clone 下来这个功能就是空的、演示不了。同时交接文档里那些数字
# （GLM 9.55 / DeepSeek 8.88 / Qwen 8.07、自评偏好 −0.63 等）也需要一份
# 可复查的原始物证，否则被追问「数从哪来」只能现场翻开发副本。
#
# ⚠ 只放行这一个时间戳目录，其余运行产物（data/syn/ 的数据集与 raw 输出、
# 以后新跑的 compare 报告）继续不入库。
#
# 下面四行看着绕，但必须这么写（亲测踩过）：
#   只写 `!data/compare/` 会把**整个** compare 目录放行，以后新跑的对比报告
#   会被静默提交进来 —— 因为 `data/*` 只匹配到 `data/compare` 这一层，
#   再深的子目录不受它管。所以必须：先放行父目录好让 git 能往下走，
#   紧接着用 `data/compare/*` 把下一代重新排除，最后只反选要的那一个。
!data/compare/
data/compare/*
!data/compare/20260911_104142/
!data/compare/20260911_104142/*

# Python 缓存
__pycache__/
*.pyc
